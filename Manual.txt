# PRE INSTALLATION STEPS

# Create partitons:

gdisk /dev/sdX

n<return>
<return>
<return>
+100M<return>
EF00<return>

n<return>
<return>
<return>
<return>
<return>

w<return>
y<return>

# Format partitons:

mkfs.vfat -F32 /dev/sdX1
mkfs.ext4 /dev/sdX2

# Mount partitons:

mount /dev/sdX2 /mnt

genfstab -U /mnt >> /mnt/etc/fstab

mkdir -p /mnt/boot/efi

mount /dev/sdX1 /mnt/boot/efi

# Pacstrap:
# (Edit /etc/pacman.d/mirrorlist if needed)

pacstrap /mnt base

# Idk

arch-chroot /mnt

# Somehow get setup-system.sh into the chroot

sh setup-system.sh

# MAKE SURE YOU GET post-install.sh INTO /home/<username> BEFORE REBOOTING

# Reboot

sudo sh post-install.sh
